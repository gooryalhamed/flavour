<!DOCTYPE html>
<html>
<head>
	<title>Sign-Up</title>
</head>
<body>
	<div class="container">
		<div class="notice"><%= notice %></div>
		
		<%= form_for(@user) do |f| %>
		<table>
		<tr>
			<td><%= f.label :name %></td>
			<td><%= f.text_field :name %></td>	
		</tr>
		<tr>
			<td><%= f.label :password %></td>
			<td><%= f.password_field :password %></td>
		</tr>
			<tr>
				<td><%= f.label :password_confirmation %></td>
				<td><%= f.password_field :password_confirmation %></td>	
			</tr>
			</table>
			<div id="signup" class="btn"><%= f.submit value: "create" %></div>
		<% end %>
		<a href="/"><input type="submit" class="btn" value="home"></a>
	</div>
</body>
</html>







